diff --git a/node_modules/y-codemirror.next/src/y-remote-selections.js b/node_modules/y-codemirror.next/src/y-remote-selections.js
index d838a96..c924ad1 100644
--- a/node_modules/y-codemirror.next/src/y-remote-selections.js
+++ b/node_modules/y-codemirror.next/src/y-remote-selections.js
@@ -217,15 +217,7 @@ export class YRemoteSelectionsPluginValue {
             class: 'cm-ySelection'
           })
         })
-        // render text-selection in the last line
-        decorations.push({
-          from: endLine.from,
-          to: end,
-          value: cmView.Decoration.mark({
-            attributes: { style: `background-color: ${colorLight}` },
-            class: 'cm-ySelection'
-          })
-        })
+        // render text-selection in the middle lines (must be pushed in order before end line)
         for (let i = startLine.number + 1; i < endLine.number; i++) {
           const linePos = update.view.state.doc.line(i).from
           decorations.push({
@@ -236,6 +228,15 @@ export class YRemoteSelectionsPluginValue {
             })
           })
         }
+        // render text-selection in the last line
+        decorations.push({
+          from: endLine.from,
+          to: end,
+          value: cmView.Decoration.mark({
+            attributes: { style: `background-color: ${colorLight}` },
+            class: 'cm-ySelection'
+          })
+        })
       }
       decorations.push({
         from: head.index,
@@ -247,6 +248,9 @@ export class YRemoteSelectionsPluginValue {
         })
       })
     })
+    // Sort by from position before building the set, since cursor widgets can appear
+    // at any position relative to selection decorations across multiple users.
+    decorations.sort((a, b) => a.from - b.from || a.value.startSide - b.value.startSide)
     this.decorations = cmView.Decoration.set(decorations, true)
   }
 }
